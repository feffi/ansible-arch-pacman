---
- name: refresh pacman cache
  pacman:
    update_cache: yes
    
- name: install pacman packages
  pacman:
    name: "{{ ansible_pacman.install }}"
    state: present
  when: ansible_pacman.install is defined and (ansible_pacman.install | length > 0)